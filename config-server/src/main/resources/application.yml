server:
  port: 8181

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/J0aoPaulo/hermes-config
          username: ${GIT_USERNAME}
          password: ${GIT_PASSWORD}